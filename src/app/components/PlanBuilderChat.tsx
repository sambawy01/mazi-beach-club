import React from 'react';

// This widget has been retired as part of the Mazi rebrand.
// Kept as a minimal stub so stale imports don't break the build.
interface PlanBuilderChatProps {
  isOpen: boolean;
  onClose: () => void;
}

export function PlanBuilderChat({ isOpen, onClose }: PlanBuilderChatProps) {
  return null;
}